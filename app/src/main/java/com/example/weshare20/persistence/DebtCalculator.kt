class DebtCalculator {
    fun calculateDebts(group: Group): Map<User, Double> {
        // Implement your algorithm to calculate debts within a group
        // Return a map of user to amount owed/owing
        return emptyMap()
    }
}